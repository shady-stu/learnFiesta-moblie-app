type Course = {
    id: string;
    title: string;
    instructor: string;
    duration?: string;
    rating: number;
    reviews: string;
    price: string;
    oldPrice?: string;
    image: string;
    badge?: string;
}

export default Course;